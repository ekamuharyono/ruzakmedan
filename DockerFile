# Gunakan image PHP dari Docker Hub
FROM php:8.0-apache

# Salin file proyek Anda ke dalam container
COPY . /var/www/html/

# Install dependensi PHP jika diperlukan
RUN docker-php-ext-install mysqli

# Expose port 80
EXPOSE 80